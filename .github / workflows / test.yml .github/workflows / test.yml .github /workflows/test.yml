name: تجربة من الهاتف

on:
  push:
    branches: [main]  # يشتغل فقط على الفرع الرئيسي

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: طباعة رسالة التجربة
        run: echo "اشتغل بنجاح من الموبايل 🔥"
      
      - name: معلومات النظام
        run: |
          echo "التاريخ: $(date)"
          echo "النظام: $(uname -a)"
          
